import 'package:flutter/material.dart';
import 'ui/home.dart';

void main() {
  runApp(x());
}

class x extends StatelessWidget {
  build(BuildContext context) {
    return MyApp();
  }
}
